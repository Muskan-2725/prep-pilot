# PrepPilot contributor guide

Keep the MVP dependency-light and runnable with Python's standard library. Treat exam facts as source-grounded data: cite a source record or explicitly state uncertainty. Do not add secrets, generated databases, or virtual environments to Git. Run `python -m pytest` before committing.
