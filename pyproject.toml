[project]
name = "preppilot"
version = "0.1.0"
description = "A grounded, adaptive GATE CSE study mentor MVP"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
